diff -ru binutils-stable-o/bfd/doc/bfd.texinfo binutils-stable-p/bfd/doc/bfd.texinfo
--- binutils-stable-o/bfd/doc/bfd.texinfo	2008-11-13 13:11:15.000000000 +0000
+++ binutils-stable-p/bfd/doc/bfd.texinfo	2014-01-31 10:36:09.815204558 +0000
@@ -323,8 +323,8 @@
 @printindex cp
 
 @tex
-% I think something like @colophon should be in texinfo.  In the
-% meantime:
+% I think something like @@colophon should be in texinfo.  In the
+ meantime:
 \long\def\colophon{\hbox to0pt{}\vfill
 \centerline{The body of this manual is set in}
 \centerline{\fontname\tenrm,}
@@ -334,7 +334,7 @@
 \centerline{{\sl\fontname\tensl\/}}
 \centerline{are used for emphasis.}\vfill}
 \page\colophon
-% Blame: doc@cygnus.com, 28mar91.
+% Blame: doc@@cygnus.com, 28mar91.
 @end tex
 
 @bye
diff -ru binutils-stable-o/bfd/doc/Makefile.in binutils-stable/bfd/doc/Makefile.in
--- binutils-stable-o/bfd/doc/Makefile.in       2008-11-13 13:11:15.000000000 +0000
+++ binutils-stable/bfd/doc/Makefile.in 2014-01-31 10:41:13.960663335 +0000
@@ -535,7 +535,8 @@

 html-am: $(HTMLS)

-info: info-am
+info:
+#      info-am

 info-am: $(INFO_DEPS)

diff -r -U5 binutils-stable-o/gas/doc/c-arm.texi binutils-stable-p/gas/doc/c-arm.texi
--- binutils-stable-o/gas/doc/c-arm.texi	2014-07-10 20:38:39.041507369 -0400
+++ binutils-stable-p/gas/doc/c-arm.texi	2014-07-10 20:45:01.983865805 -0400
@@ -18,10 +18,11 @@
 @cindex Thumb support
 @menu
 * ARM Options::              Options
 * ARM Syntax::               Syntax
 * ARM Floating Point::       Floating Point
+* ARM Relocations::          Relocations
 * ARM Directives::           ARM Machine Directives
 * ARM Opcodes::              Opcodes
 * ARM Mapping Symbols::      Mapping Symbols
 @end menu
 
@@ -299,11 +300,10 @@
 @node ARM Syntax
 @section Syntax
 @menu
 * ARM-Chars::                Special Characters
 * ARM-Regs::                 Register Names
-* ARM-Relocations::	     Relocations
 @end menu
 
 @node ARM-Chars
 @subsection Special Characters
 
@@ -339,12 +339,12 @@
 
 @cindex floating point, ARM (@sc{ieee})
 @cindex ARM floating point (@sc{ieee})
 The ARM family uses @sc{ieee} floating-point numbers.
 
-@node ARM-Relocations
-@subsection ARM relocation generation
+@node ARM Relocations
+@section Relocations 
 
 @cindex data relocations, ARM
 @cindex ARM data relocations
 Specific data relocations can be generated by putting the relocation name
 in parentheses after the symbol name.  For example:
diff -r -U5 binutils-stable-o/gas/doc/c-tic54x.texi binutils-stable-p/gas/doc/c-tic54x.texi
--- binutils-stable-o/gas/doc/c-tic54x.texi	2014-07-10 20:59:09.737036986 -0400
+++ binutils-stable-p/gas/doc/c-tic54x.texi	2014-07-10 21:00:24.307664167 -0400
@@ -106,11 +106,11 @@
 
 In this example, x is replaced with SYM2; SYM2 is replaced with SYM1, and SYM1
 is replaced with x.  At this point, x has already been encountered
 and the substitution stops.
 
-@smallexample @code
+@smallexample @code{*}
  .asg   "x",SYM1 
  .asg   "SYM1",SYM2
  .asg   "SYM2",x
  add    x,a             ; final code assembled is "add  x, a"
 @end smallexample
@@ -123,18 +123,18 @@
 
 Substitution may be forced in situations where replacement might be
 ambiguous by placing colons on either side of the subsym.  The following
 code: 
 
-@smallexample @code
+@smallexample @code{*}
  .eval  "10",x
 LAB:X:  add     #x, a
 @end smallexample
 
 When assembled becomes:
 
-@smallexample @code
+@smallexample @code{*}
 LAB10  add     #10, a
 @end smallexample
 
 Smaller parts of the string assigned to a subsym may be accessed with
 the following syntax:
@@ -306,11 +306,11 @@
 @section Extended Addressing
 The @code{LDX} pseudo-op is provided for loading the extended addressing bits
 of a label or address.  For example, if an address @code{_label} resides
 in extended program memory, the value of @code{_label} may be loaded as
 follows:
-@smallexample @code
+@smallexample @code{*}
  ldx     #_label,16,a    ; loads extended bits of _label
  or      #_label,a       ; loads lower 16 bits of _label
  bacc    a               ; full address is in accumulator A
 @end smallexample
 
diff -r -U5 binutils-stable-o/bfd/elf.c binutils-stable-p/bfd/elf.c
--- binutils-stable-o/bfd/elf.c	2014-07-10 21:07:37.708439792 -0400
+++ binutils-stable-p/bfd/elf.c	2014-07-10 21:08:58.822142462 -0400
@@ -29,11 +29,15 @@
 	Currently, the best supported back ends are for sparc and i386
 	(running svr4 or Solaris 2).
 
 	Documentation of the internals of the support code still needs
 	to be written.  The code is changing quickly enough that we
-	haven't bothered yet.  */
+	haven't bothered yet.
+
+SUBSECTION
+	ELF backend functions
+*/
 
 /* For sparc64-cross-sparc32.  */
 #define _SYSCALL32
 #include "sysdep.h"
 #include "bfd.h"
diff -r -U5 binutils-stable/gas/doc/c-mips.texi binutils-stable-p/gas/doc/c-mips.texi
--- binutils-stable/gas/doc/c-mips.texi	2008-11-13 08:10:38.000000000 -0500
+++ binutils-stable-p/gas/doc/c-mips.texi	2014-07-10 21:16:56.117250310 -0400
@@ -200,11 +200,11 @@
 Generate code for the MIPS @sc{r4650} chip.  This tells the assembler to accept
 the @samp{mad} and @samp{madu} instruction, and to not schedule @samp{nop}
 instructions around accesses to the @samp{HI} and @samp{LO} registers.
 @samp{-no-m4650} turns off this option.
 
-@itemx -m3900
+@item -m3900
 @itemx -no-m3900
 @itemx -m4100
 @itemx -no-m4100
 For each option @samp{-m@var{nnnn}}, generate code for the MIPS
 @sc{r@var{nnnn}} chip.  This tells the assembler to accept instructions
diff -r -U5 binutils-stable/gas/doc/c-tic54x.texi binutils-stable-p/gas/doc/c-tic54x.texi
--- binutils-stable/gas/doc/c-tic54x.texi	2014-07-10 21:15:27.576493555 -0400
+++ binutils-stable-p/gas/doc/c-tic54x.texi	2014-07-10 21:16:19.588938407 -0400
@@ -342,11 +342,11 @@
 @item .asg @var{string}, @var{name}
 Assign @var{name} the string @var{string}.  String replacement is
 performed on @var{string} before assignment.
 
 @cindex @code{eval} directive, TIC54X
-@itemx .eval @var{string}, @var{name}
+@item .eval @var{string}, @var{name}
 Evaluate the contents of string @var{string} and assign the result as a
 string to the subsym @var{name}.  String replacement is performed on
 @var{string} before assignment. 
 
 @cindex @code{bss} directive, TIC54X
diff -r -U5 binutils-stable/gas/doc/c-arc.texi binutils-stable-p/gas/doc/c-arc.texi
--- binutils-stable/gas/doc/c-arc.texi	2008-11-13 08:10:38.000000000 -0500
+++ binutils-stable-p/gas/doc/c-arc.texi	2014-07-10 21:27:02.099378258 -0400
@@ -210,11 +210,11 @@
 The ARCtangent A4 allows the user to specify extension instructions.
 The extension instructions are not macros.  The assembler creates
 encodings for use of these instructions according to the specification
 by the user.  The parameters are:
 
-@table @bullet
+@table @code
 @item @var{name}
 Name of the extension instruction 
 
 @item @var{opcode}
 Opcode to be used. (Bits 27:31 in the encoding).  Valid values
diff -r -U5 binutils-stable/ld/ld.texinfo binutils-stable-p/ld/ld.texinfo
--- binutils-stable/ld/ld.texinfo	2008-11-13 08:10:36.000000000 -0500
+++ binutils-stable-p/ld/ld.texinfo	2014-07-10 22:03:46.723117279 -0400
@@ -1746,11 +1746,11 @@
 supported.  Display which input files can and cannot be opened.  Display
 the linker script being used by the linker.
 
 @kindex --version-script=@var{version-scriptfile}
 @cindex version script, symbol versions
-@itemx --version-script=@var{version-scriptfile}
+@item --version-script=@var{version-scriptfile}
 Specify the name of a version script to the linker.  This is typically
 used when creating shared libraries to specify additional information
 about the version hierarchy for the library being created.  This option
 is only meaningful on ELF platforms which support shared libraries.
 @xref{VERSION}.
@@ -7077,20 +7077,20 @@
 @unnumbered LD Index
 
 @printindex cp
 
 @tex
-% I think something like @colophon should be in texinfo.  In the
+% I think something like @@colophon should be in texinfo.  In the
 % meantime:
 \long\def\colophon{\hbox to0pt{}\vfill
 \centerline{The body of this manual is set in}
 \centerline{\fontname\tenrm,}
 \centerline{with headings in {\bf\fontname\tenbf}}
 \centerline{and examples in {\tt\fontname\tentt}.}
 \centerline{{\it\fontname\tenit\/} and}
 \centerline{{\sl\fontname\tensl\/}}
 \centerline{are used for emphasis.}\vfill}
 \page\colophon
-% Blame: doc@cygnus.com, 28mar91.
+% Blame: doc@@cygnus.com, 28mar91.
 @end tex
 
 @bye
diff -r -U5 binutils-stable/bfd/doc/Makefile.in binutils-stable-p/bfd/doc/Makefile.in
--- binutils-stable/bfd/doc/Makefile.in	2008-11-13 08:11:15.000000000 -0500
+++ binutils-stable-p/bfd/doc/Makefile.in	2014-07-10 23:10:30.092711214 -0400
@@ -488,11 +488,11 @@
 ctags: CTAGS
 CTAGS:
 
 check-am:
 check: check-am
-all-am: Makefile
+all-am: Makefile $(bfd_TEXINFOS)
 installdirs:
 install-exec: install-exec-am
 install-data: install-data-am
 uninstall: uninstall-am
 
diff -r -U5 binutils-stable/Makefile.in binutils-stable-p/Makefile.in
--- binutils-stable/Makefile.in	2008-11-13 08:11:25.000000000 -0500
+++ binutils-stable-p/Makefile.in	2014-07-11 08:57:10.034883972 -0400
@@ -2469,11 +2469,12 @@
 install:
 	@: $(MAKE); $(unstage)
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s; \
 	$(MAKE) $(RECURSE_FLAGS_TO_PASS) installdirs \
-	install-host install-target install-html install-pdf
+	install-host install-target install-html
+# install-pdf
 
 .PHONY: install-host-nogcc
 install-host-nogcc:  \
     maybe-install-ash \
     maybe-install-autoconf \
diff -r -U5 binutils-stable/libiberty/testsuite/test-demangle.c binutils-stable-p/libiberty/testsuite/test-demangle.c
--- binutils-stable/libiberty/testsuite/test-demangle.c	2008-11-13 08:11:09.000000000 -0500
+++ binutils-stable-p/libiberty/testsuite/test-demangle.c	2014-07-11 09:20:51.165597922 -0400
@@ -44,11 +44,11 @@
 /* Safely read a single line of arbitrary length from standard input.  */
 
 #define LINELEN 80
 
 static void
-getline(buf)
+_getline(buf)
      struct line *buf;
 {
   char *data = buf->data;
   size_t alloc = buf->alloced;
   size_t count = 0;
@@ -194,16 +194,16 @@
 
   for (;;)
     {
       const char *inp;
       
-      getline (&format);
+      _getline (&format);
       if (feof (stdin))
 	break;
 
-      getline (&input);
-      getline (&expect);
+      _getline (&input);
+      _getline (&expect);
 
       inp = protect_end (input.data);
 
       tests++;
 
@@ -320,11 +320,11 @@
 	}
       free (result);
 
       if (no_params)
 	{
-	  getline (&expect);
+	  _getline (&expect);
 	  result = cplus_demangle (inp, DMGL_ANSI|DMGL_TYPES);
 
 	  if (result
 	      ? strcmp (result, expect.data)
 	      : strcmp (input.data, expect.data))

